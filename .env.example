# =============================================================================
# CONFIGURAZIONE MYGEST - Environment Variables
# =============================================================================
# ISTRUZIONI: Copia questo file in .env e personalizza i valori

# -----------------------------------------------------------------------------
# DJANGO CORE (⚠️ OBBLIGATORIO PER PRODUZIONE)
# -----------------------------------------------------------------------------
DEBUG=False
SECRET_KEY=CHANGE-THIS-TO-A-LONG-RANDOM-STRING-IN-PRODUCTION
ALLOWED_HOSTS=localhost,127.0.0.1,72.62.34.249

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:password@localhost:5432/mygest

# -----------------------------------------------------------------------------
# ARCHIVIO / STORAGE
# -----------------------------------------------------------------------------
# Percorso base per l'archiviazione dei file
# - Ambiente di sviluppo (WSL): /mnt/archivio (montaggio NAS)
# - Ambiente produzione (VPS): /srv/mygest/archivio
ARCHIVIO_BASE_PATH=/srv/mygest/archivio

# -----------------------------------------------------------------------------
# EMAIL SMTP
# -----------------------------------------------------------------------------
EMAIL_HOST=smtp.aruba.it
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
EMAIL_HOST_USER=your_email@example.com
EMAIL_HOST_PASSWORD=your_password
DEFAULT_FROM_EMAIL=your_email@example.com
EMAIL_TIMEOUT=30

# -----------------------------------------------------------------------------
# EMAIL IMAP (per archiviazione comunicazioni)
# -----------------------------------------------------------------------------
IMAP_HOST=imaps.aruba.it
IMAP_PORT=993
IMAP_USE_SSL=true
IMAP_USERNAME=your_email@example.com
IMAP_PASSWORD=your_password
IMAP_FOLDER=INBOX
IMAP_TIMEOUT=60

# -----------------------------------------------------------------------------
# WHATSAPP BUSINESS API
# -----------------------------------------------------------------------------
# WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
# WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
# WHATSAPP_ACCESS_TOKEN=your_access_token
# WHATSAPP_VERIFY_TOKEN=your_verify_token
# WHATSAPP_CLOUD_TIMEOUT=10

# -----------------------------------------------------------------------------
# IMPORTAZIONI
# -----------------------------------------------------------------------------
# IMPORTAZIONI_SOURCE_DIRS=/path/to/source1,/path/to/source2

